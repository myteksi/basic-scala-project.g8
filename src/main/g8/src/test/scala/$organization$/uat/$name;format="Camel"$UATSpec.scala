package $organization$.$name;format="lower"$
package uat

import org.scalatest.FeatureSpec

class $name;format="Camel"$UATSpec extends FeatureSpec {
  feature("This is the feature / user story") {
    scenario("this is the first scenario") (pending)

    scenario("this is the second scenario") (pending)

  }
}
